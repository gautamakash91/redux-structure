const initial_state = {
  signup_email: "",
  signup_password: "",
  signup_confirm_password: ""
};

export default function reducer(state = initial_state, action) {
  switch (action.type) {
    default:
      return state;
  }
}